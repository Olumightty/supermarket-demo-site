import { CheckoutForm } from '@/components/CheckoutForm';

export const metadata = {
  title: 'Checkout | MarketSim',
};

export default function CheckoutPage() {
  return (
    <div>
      <CheckoutForm />
    </div>
  );
}

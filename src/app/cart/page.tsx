import { CartView } from '@/components/CartView';

export const metadata = {
  title: 'Your Cart | MarketSim',
};

export default function CartPage() {
  return (
    <div>
      <CartView />
    </div>
  );
}
